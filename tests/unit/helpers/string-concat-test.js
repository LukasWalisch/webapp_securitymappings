import { stringConcat } from 'webapp-securitymappings/helpers/string-concat';
import { module, test } from 'qunit';

module('Unit | Helper | string concat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringConcat([42]);
  assert.ok(result);
});
