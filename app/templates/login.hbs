{{#if isLogged}}
You are logged in alreay, switch to Rate/Navigation
{{else}}
<div class="loginRegister">
    <form {{action 'authenticate' on='submit'}}>
        <div class="form-group">
            <h2>Login</h2>
            <label for="exampleInputEmail1">Username</label>
            {{input valueBinding="login" placeholder='Login'}}<br>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            {{input valueBinding="loginPassword" placeholder='password' type='password'}}<br>
        </div>
        <button type="submit" class="btn btn-default">Login</button>
    </form>
</div>
<div class="loginRegister">
    <form {{action 'register' on='submit'}}>
        <div class="form-group">
            <h2>Register</h2>
            <label for="exampleInputEmail1">Username</label>
            {{input value=register placeholder='Login'}}<br>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            {{input value=registerPassword placeholder='password' type='password'}}<br>
        </div>
        <button type="submit" class="btn btn-default">Login</button>
    </form>
</div>
{{#if showModal}}
Huhu
{{/if}}
{{/if}}